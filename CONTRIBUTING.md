# Contributing

Issues and focused pull requests are welcome. Describe the physical or economic assumption being changed, preserve units, and add a deterministic regression test for every calculation change.

Run `ruff check .`, `ruff format --check .`, and `pytest -q` before submitting. Do not commit credentials or restricted data, and do not describe internal consistency tests as external validation.
