# Verification and validation status

The automated suite checks component bounds, below-minimum-load shutdown, pipeline feasibility, solver termination, wind limits, minimum load, and a deterministic full-load dispatch case. These are software and model-consistency verification tests.

The repository does not currently provide independent validation against measured plant, market, or pipeline data. Outputs must not be called externally validated until a documented comparison with independent evidence exists.
